# Vulnerable Portal - example environment file
# NOTE: the vulnerable app currently hardcodes SECRET_KEY in app.py on
# purpose (see comment there) as an additional demonstrated weakness.
# This file is provided for structural parity with secure_portal/.env.example.
FLASK_ENV=development
FLASK_DEBUG=1
